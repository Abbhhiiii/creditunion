# Backend URL (no trailing slash). On Vercel, set this in Project Settings → Environment Variables.
# Example after Render deploy: https://creditassist-backend.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:5000
